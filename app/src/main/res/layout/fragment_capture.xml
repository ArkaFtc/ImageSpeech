<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.camera.view.PreviewView
        android:id="@+id/viewFinder"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:background="#99000000"
        android:padding="16dp">

        <TextView
            android:id="@+id/txtCaptureStatus"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:text="@string/capture_hint" />

        <!--
          Deliberately oversized. The target has to be findable without sight, and there is only one
          thing to do on this screen, so it gets the whole width and a thumb-sized height rather
          than a camera-app shutter dot.
        -->
        <Button
            android:id="@+id/btnShutter"
            android:layout_width="match_parent"
            android:layout_height="96dp"
            android:layout_marginTop="12dp"
            android:textSize="20sp"
            android:text="@string/take_photo"
            android:contentDescription="@string/take_photo_description" />

    </LinearLayout>

</FrameLayout>
